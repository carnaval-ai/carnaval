# carnaval - PII anonymization framework with reversible vault
# Apache 2.0

# Core (Phase 1)
pycryptodome>=3.20         # AES-256-GCM vault
PyYAML>=6.0                # YAML config
python-dotenv>=1.0         # .env loader
structlog>=24.1            # logs structures
lingua-language-detector>=2.0  # detection langue (remplace fasttext)

# AI (Phase 2)
gliner>=0.2                # NER PII zero-shot

# Tests
pytest>=8.0
pytest-cov>=5.0
